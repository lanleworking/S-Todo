/**
 * Number of minutes before a pending payment is automatically cancelled by the server.
 * Must match the server-side PAYMENT_EXPIRE_MINUTES constant.
 */
export const PAYMENT_EXPIRE_MINUTES = 30
